name: apollon
description: "A new Flutter project."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.11.1

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  google_fonts: 8.1.0
  http: ^1.2.0
  lottie: ^3.1.0 # HIER hinzufügen
  provider: ^6.1.2
  flutter_svg: ^2.0.10
  intl: ^0.20.2

dev_dependencies:
  flutter_test:
    sdk: flutter
flutter:
  uses-material-design: true
  assets:
    - assets/images/
    - assets/lottie/
    - assets/svgs/